function () {
  const canvas = document.getElementById('canvas');
  const ctx = canvas.getContext('2d');
  x=0;
  y=0;
  while () {
    ctx.beginPath();
    ctx.rect(x,y,1,1);
    ctx.stroke();
}
    }
